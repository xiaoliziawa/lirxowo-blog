import type { ProfileConfig } from "../types/config";

// 个人资料配置
export const profileConfig: ProfileConfig = {
	// GitHub 头像（xiaoliziawa），已下载到本地，避免外链暴露访客 IP
	avatar: "/assets/home/avatar.jpg",
	name: "LirxOwO",
	bio: "Welcome！",
	typewriter: {
		enable: true,
		speed: 80,
	},
	links: [],
};
